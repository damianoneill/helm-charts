apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservice-base.fullname" . }}-test-connection"
  labels:
    {{- include "microservice-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microservice-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
